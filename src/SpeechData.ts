export type SpeechData = [string, SpeechTopic, Date, number];

export enum SpeechTopic {
    EducationPolicy = "Education Policy",
    CoalSubsidies = "Coal Subsidies",
    InternalSecurity = "Internal Security"
}